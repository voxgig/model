# The banned list. Entries are REGEXES, matched case-insensitively by
# Vale, and each one fails CI as Vale.Avoid.
#
# THIS FILE IS THE SINGLE SOURCE OF TRUTH. tools/check_prose.py reads it
# rather than holding its own copy, so the fast local gate and the CI
# prose gate cannot disagree. Add a phrase here and both pick it up.
#
# CARRIED OVER FROM voxgig/struct, and through it from jostraca/jostraca,
# unchanged. The repositories share an author and a house voice, so they
# share the list rather than each drifting its own copy; the sources
# behind it are that project's original house list plus claudisms.ai, a
# catalogue of the patterns that mark machine-written prose.
#
# EVERY ENTRY WAS MEASURED AGAINST THIS PROJECT'S PAGES BEFORE IT WAS
# ADOPTED, because a list that fires on the subject matter is a list
# people learn to switch off. What fired, and what was done about it, is
# recorded in STYLE-GUIDE.md ("Banned phrases and patterns"). Nothing was
# dropped from the list to make it pass.
#
# Entries that would collide with this project's vocabulary are absent for
# the same reason they are absent upstream -- see "What is not banned, and
# why" in STYLE-GUIDE.md.
# --- Filler and false emphasis -----------------------------------------
worth noting
important to note
it cannot be overstated
at its core
when it comes to
let'?s break it down
here'?s where it gets interesting
the point is
because it matters

# --- Inflated vocabulary -----------------------------------------------
delve
dive into
robust
seamless(?:ly)?
comprehensive(?:ly)?
holistic
intricate
leverag(?:e|es|ed|ing)
foster(?:s|ed|ing)?
shed(?:s|ding)? light on
pav(?:e|es|ed|ing) the way
pivotal
transformative
game.chang(?:er|ing)
cutting.edge
groundbreaking
testament to
paradigm shift
realm
landscape (?:of|for)
underscor(?:es|ing) the
lean(?:s|ing)? (?:in)?to
throughline
double.click on
mature setup

# --- Consultant register -----------------------------------------------
north star
key takeaways
best practices
at the end of the day
pressure.test
right.size
strategic imperative
three things to (?:know|understand)
dispatches from
best operators
lessons learned

# --- Metaphor inflation ------------------------------------------------
load.bearing
heavy lifting
is doing the work
different physics
rules of physics
(?:hits|lands|strikes) hardest
quietly

# --- The contrast frame and its cousins --------------------------------
not just
not only .{0,40}\bbut\b
it'?s not about
the whole game
the entire point
the only thing that (?:matters|changed)

# --- False singularity and crowned superlatives ------------------------
the right (?:way|answer|tool|question)
the best thing you can do
if I had to pick
what (?:struck|got) me
stuck with me
struck a chord
(?:hit|struck) a nerve
we'?ve seen this movie
we'?ve been here before

# --- Reflective pose ---------------------------------------------------
sit with
worth (?:exploring|considering|a look|asking|talking about)
keeps? coming back to
that'?s the tell
where I landed

# --- Invented observation about people ---------------------------------
most people
everyone I'?ve worked with
a lot of folks
nobody I know

# --- Requires approval per use -----------------------------------------
#
# Not filler in the way the rest of this list is: the word is fine in
# ordinary English. It is here because it had become a verbal tic across
# both repositories -- "the honest word", "the honest complication", "the
# corpus that keeps them honest" -- where it was doing no work that the
# surrounding sentence was not already doing. Uses were removed
# upstream the day the line was added, and in voxgig/struct after it; what
# it found here is recorded in STYLE-GUIDE.md. Each was the sentence
# flattering itself rather than describing the thing.
#
# There is no inline exemption for it, deliberately: an escape hatch that
# exists gets used. A use the author wants kept is approved by editing
# this line, which is visible in review.
honest(y|ly)?

# --- Signposting -------------------------------------------------------
let'?s explore
now let'?s turn to
moving on to
in today'?s rapidly evolving
reflecting a broader trend
marking a significant shift
great question
